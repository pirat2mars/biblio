<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_home')}}">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

            {# {% if is_granted("ROLE_USER")%} #}
            {# {% if is_granted("IS_AUTHENTICATED_FULLY")%} #}
            {% if app.user %}            
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_espace_index')}}" >{{app.user.pseudo}}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_logout')}}"><i class="fa fa-user-logout"></i>Deconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{path('app_login')}}"><i class="fa fa-sign-in-alt"></i>Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_register')}}"><i class="fa fa-user-plus"></i>Inscription</a>
                </li>
            {% endif %}    
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Test
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{path('app_auteur_test')}}">Liste auteur</a></li>
                        <li><a class="dropdown-item" href="{{path('app_auteur_test_add')}}">Ajouter auteur auto</a></li>
                        <li><a class="dropdown-item" href="{{path('app_auteur_test_ajouter')}}">Ajouter auteur</a></li>

                    </ul>
                </li>

                {# CRUD #}
                {% if is_granted("ROLE_ADMIN")%}


                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Abonnés
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_abonne_index')}}">
                                <i class="fa fa-list"></i> Liste
                            </a></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_abonne_new') }}">
                                <i class="fa fa-plus"></i> Ajouter
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Livres
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_livre_index')}}">
                                <i class="fa fa-list"></i> Liste
                            </a></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_livre_new') }}">
                                <i class="fa fa-plus"></i> Ajouter
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Auteurs
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_auteur_index')}}">
                                <i class="fa fa-list"></i> Liste
                            </a></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_auteur_new') }}">
                                <i class="fa fa-plus"></i> Ajouter
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Genres
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_genre_index')}}">
                                <i class="fa fa-list"></i> Liste
                            </a></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_genre_new') }}">
                                <i class="fa fa-plus"></i> Ajouter
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Emprunt
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_emprunt_index')}}">
                                <i class="fa fa-list"></i> Liste
                            </a></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_admin_emprunt_new') }}">
                                <i class="fa fa-plus"></i> Ajouter
                            </a>
                        </li>
                    </ul>
                </li>



                {% endif %}

            <form class="d-flex" action="{{path('app_recherche')}}">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>